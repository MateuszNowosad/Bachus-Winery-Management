const ScrollableDialogFormStyle = () => ({
  dialog: {
    position: 'fixed',
    width: 'auto',
    overflow: 'auto'
  },
  root: {
      display:'flex',
      maxWidth: '100%',
      justifyContent: 'center',
  }
});

export default ScrollableDialogFormStyle;
